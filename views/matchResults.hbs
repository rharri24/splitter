<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Match Results - Splitter</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      margin: 0;
      padding: 20px;
    }

    .match-results-container {
      max-width: 900px;
      margin: 0 auto;
      padding: 40px 20px;
    }

    .header-section {
      background: var(--surface);
      border-radius: 12px;
      padding: 32px;
      margin-bottom: 32px;
      box-shadow: 0 4px 16px var(--shadow);
      text-align: center;
    }

    .header-section h1 {
      margin: 0 0 16px 0;
      color: var(--text-main);
      font-size: 32px;
    }

    .destination-badge {
      display: inline-block;
      background: var(--primary);
      color: white;
      padding: 8px 20px;
      border-radius: 20px;
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 16px;
    }

    .search-info {
      color: var(--text-muted);
      font-size: 16px;
    }

    .match-count {
      background: linear-gradient(135deg, var(--primary), var(--accent));
      color: white;
      padding: 20px;
      border-radius: 12px;
      text-align: center;
      margin-bottom: 32px;
      font-size: 20px;
      font-weight: 600;
    }

    .no-matches {
      background: var(--surface);
      border: 2px dashed var(--border);
      border-radius: 12px;
      padding: 48px;
      text-align: center;
      color: var(--text-muted);
    }

    .no-matches h2 {
      color: var(--text-main);
      margin-bottom: 12px;
    }

    .match-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-left: 5px solid var(--primary);
      border-radius: 12px;
      padding: 24px;
      margin-bottom: 20px;
      box-shadow: 0 2px 8px var(--shadow);
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    .match-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }

    .match-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      flex-wrap: wrap;
      gap: 12px;
    }

    .match-name {
      font-size: 24px;
      font-weight: 700;
      color: var(--text-main);
      margin: 0;
    }

    .time-badge {
      background: #f0fdf4;
      color: #15803d;
      padding: 6px 14px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: 600;
    }

    .match-details {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 16px;
      margin-bottom: 20px;
    }

    .detail-item {
      display: flex;
      align-items: start;
      gap: 12px;
    }

    .detail-icon {
      font-size: 20px;
      margin-top: 2px;
    }

    .detail-content {
      flex: 1;
    }

    .detail-label {
      font-size: 12px;
      text-transform: uppercase;
      color: var(--text-muted);
      font-weight: 600;
      letter-spacing: 0.5px;
      margin-bottom: 4px;
    }

    .detail-value {
      font-size: 16px;
      color: var(--text-main);
      font-weight: 500;
    }

    .contact-section {
      background: #f9fafb;
      border-radius: 8px;
      padding: 16px;
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .contact-btn {
      flex: 1;
      min-width: 200px;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: 600;
      text-decoration: none;
      text-align: center;
      transition: all 0.2s ease;
      font-size: 15px;
    }

    .email-btn {
      background: var(--primary);
      color: white;
    }

    .email-btn:hover {
      background: var(--primary-dark);
      transform: translateY(-2px);
    }

    .phone-btn {
      background: #10b981;
      color: white;
    }

    .phone-btn:hover {
      background: #059669;
      transform: translateY(-2px);
    }

    .back-section {
      text-align: center;
      margin-top: 48px;
      padding-top: 32px;
      border-top: 2px solid var(--border);
    }

    .back-btn {
      display: inline-block;
      padding: 14px 32px;
      background: var(--surface);
      border: 2px solid var(--primary);
      color: var(--primary);
      border-radius: 8px;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.2s ease;
    }

    .back-btn:hover {
      background: var(--primary);
      color: white;
    }

    @media (max-width: 768px) {
      .match-header {
        flex-direction: column;
        align-items: flex-start;
      }

      .contact-btn {
        min-width: 100%;
      }

      .match-details {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="match-results-container">
    <!-- Header Section -->
    <div class="header-section">
      <h1>🚗 Rideshare Matches Found</h1>
      <div class="destination-badge">📍 {{destination}}</div>
      <p class="search-info">Your arrival time: {{arrivalTime}}</p>
    </div>

    <!-- Match Count -->
    {{#if matches.length}}
      <div class="match-count">
        {{#ifCond matchCount '===' 1}}
          🎉 Found 1 potential rideshare partner!
        {{else}}
          🎉 Found {{matchCount}} potential rideshare partners!
        {{/ifCond}}
      </div>

      <!-- Match Cards -->
      {{#each matches}}
        <div class="match-card">
          <div class="match-header">
            <h2 class="match-name">{{this.studentName}}</h2>
            {{#if this.timeDiff}}
              <span class="time-badge">
                {{#ifCond this.timeDiff '===' 0}}
                  ⏰ Same time!
                {{else}}
                  ⏰ {{this.timeDiff}} min difference
                {{/ifCond}}
              </span>
            {{/if}}
          </div>

          <div class="match-details">
            <div class="detail-item">
              <span class="detail-icon">🕐</span>
              <div class="detail-content">
                <div class="detail-label">Arrival Time</div>
                <div class="detail-value">{{this.arrivalTime}}</div>
              </div>
            </div>

            <div class="detail-item">
              <span class="detail-icon">📧</span>
              <div class="detail-content">
                <div class="detail-label">Email</div>
                <div class="detail-value">{{this.studentEmail}}</div>
              </div>
            </div>

            <div class="detail-item">
              <span class="detail-icon">📱</span>
              <div class="detail-content">
                <div class="detail-label">Phone</div>
                <div class="detail-value">{{this.studentPhone}}</div>
              </div>
            </div>
          </div>

          <div class="contact-section">
            <a href="mailto:{{this.studentEmail}}" class="contact-btn email-btn">
              ✉️ Email {{this.studentName}}
            </a>
            <a href="tel:{{this.studentPhone}}" class="contact-btn phone-btn">
              📞 Call {{this.studentPhone}}
            </a>
          </div>
        </div>
      {{/each}}

    {{else}}
      <!-- No Matches -->
      <div class="no-matches">
        <h2>😔 No Matches Found</h2>
        <p>No one else is heading to {{destination}} around {{arrivalTime}}.</p>
        <p>Check back later or try a different time!</p>
      </div>
    {{/if}}

    <!-- Back Button -->
    <div class="back-section">
      <a href="/mainPage?email={{email}}&uid={{uid}}" class="back-btn">
        ← Back to Dashboard
      </a>
    </div>
  </div>
</body>
</html>
